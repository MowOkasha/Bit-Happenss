<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>My Wishlist - Travel World</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css"
        integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root {
            --primary-dark: #0a0e27;
            --secondary-dark: #151a35;
            --accent-blue: #00d4ff;
            --accent-teal: #1dd1a1;
            --accent-gold: #ffd700;
        }
        
        * { margin: 0; padding: 0; box-sizing: border-box; }
        
        body {
            background: var(--primary-dark);
            background-image: radial-gradient(circle at 20% 50%, rgba(0, 212, 255, 0.1) 0%, transparent 50%),
                radial-gradient(circle at 80% 80%, rgba(29, 209, 161, 0.1) 0%, transparent 50%);
            background-attachment: fixed;
            min-height: 100vh;
            color: #ffffff;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
        
        .glass-nav {
            position: fixed; top: 0; left: 0; right: 0; z-index: 1000;
            background: rgba(21, 26, 53, 0.7); backdrop-filter: blur(20px);
            border-bottom: 1px solid rgba(255, 255, 255, 0.1); padding: 15px 0;
        }
        
        .nav-container {
            max-width: 1400px; margin: 0 auto; padding: 0 30px;
            display: flex; align-items: center; justify-content: space-between;
        }
        
        .logo { display: flex; align-items: center; gap: 12px; font-size: 26px;
            font-weight: 800; color: #ffffff; text-decoration: none; }
        
        .logo-icon {
            width: 40px; height: 40px;
            background: linear-gradient(135deg, var(--accent-blue), var(--accent-teal));
            border-radius: 10px; display: flex; align-items: center; justify-content: center;
        }
        
        .nav-links { display: flex; align-items: center; gap: 30px; }
        
        .nav-link-item {
            color: rgba(255, 255, 255, 0.8); text-decoration: none; font-weight: 600;
            transition: all 0.3s ease;
        }
        
        .nav-link-item:hover { color: var(--accent-blue); text-decoration: none; }
        
        .user-avatar {
            width: 42px; height: 42px; border-radius: 50%;
            background: linear-gradient(135deg, var(--accent-blue), var(--accent-teal));
            display: flex; align-items: center; justify-content: center; cursor: pointer;
            font-weight: 700; border: 2px solid rgba(255, 255, 255, 0.2);
            transition: all 0.3s ease;
        }
        
        .user-avatar:hover { transform: scale(1.1); box-shadow: 0 0 20px rgba(0, 212, 255, 0.5); }
        
        .user-dropdown { position: relative; }
        
        .dropdown-menu-custom {
            position: absolute; top: 55px; right: 0; background: rgba(21, 26, 53, 0.95);
            backdrop-filter: blur(20px); border-radius: 15px; padding: 15px; min-width: 200px;
            box-shadow: 0 10px 40px rgba(0, 0, 0, 0.5); border: 1px solid rgba(255, 255, 255, 0.1);
            display: none;
        }
        
        .dropdown-menu-custom.show { display: block; }
        
        .dropdown-item-custom {
            padding: 12px 15px; color: rgba(255, 255, 255, 0.9); text-decoration: none;
            display: block; border-radius: 8px; transition: all 0.3s ease;
        }
        
        .dropdown-item-custom:hover {
            background: rgba(0, 212, 255, 0.1); color: var(--accent-blue); text-decoration: none;
        }
        
        .page-header {
            margin-top: 100px;
            padding: 60px 30px 40px;
            text-align: center;
        }
        
        .page-title {
            font-size: 52px;
            font-weight: 900;
            background: linear-gradient(135deg, #ffffff, var(--accent-teal));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            margin-bottom: 15px;
        }
        
        .page-subtitle {
            font-size: 20px;
            color: rgba(255, 255, 255, 0.7);
            margin-bottom: 20px;
        }
        
        .wishlist-stats {
            display: inline-flex;
            align-items: center;
            gap: 10px;
            background: rgba(29, 209, 161, 0.1);
            padding: 12px 24px;
            border-radius: 25px;
            border: 1px solid rgba(29, 209, 161, 0.2);
        }
        
        .wishlist-container {
            max-width: 1200px;
            margin: 40px auto;
            padding: 0 30px 80px;
        }
        
        .wishlist-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(350px, 1fr));
            gap: 25px;
        }
        
        .wishlist-card {
            background: var(--secondary-dark);
            border-radius: 20px;
            overflow: hidden;
            cursor: pointer;
            transition: all 0.4s ease;
            border: 1px solid rgba(255, 255, 255, 0.1);
            display: flex;
            flex-direction: column;
        }
        
        .wishlist-card:hover {
            transform: translateY(-8px);
            box-shadow: 0 15px 40px rgba(29, 209, 161, 0.3);
            border-color: var(--accent-teal);
        }
        
        .card-image-wrapper {
            position: relative;
            height: 220px;
            overflow: hidden;
        }
        
        .card-image {
            width: 100%;
            height: 100%;
            object-fit: cover;
            transition: transform 0.4s ease;
        }
        
        .wishlist-card:hover .card-image {
            transform: scale(1.1);
        }
        
        .card-category {
            position: absolute;
            top: 15px;
            right: 15px;
            background: rgba(0, 212, 255, 0.9);
            backdrop-filter: blur(10px);
            padding: 6px 16px;
            border-radius: 20px;
            font-size: 12px;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 1px;
        }
        
        .card-body-wishlist {
            padding: 25px;
            flex: 1;
            display: flex;
            flex-direction: column;
        }
        
        .wishlist-name {
            font-size: 24px;
            font-weight: 800;
            color: #ffffff;
            margin-bottom: 12px;
        }
        
        .wishlist-description {
            font-size: 14px;
            color: rgba(255, 255, 255, 0.6);
            line-height: 1.6;
            margin-bottom: 20px;
            flex: 1;
        }
        
        .card-actions {
            display: flex;
            gap: 10px;
        }
        
        .view-detail-btn {
            flex: 1;
            background: linear-gradient(135deg, var(--accent-blue), var(--accent-teal));
            border: none;
            padding: 12px 20px;
            border-radius: 25px;
            color: #ffffff;
            font-weight: 700;
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            gap: 8px;
            transition: all 0.3s ease;
        }
        
        .view-detail-btn:hover {
            transform: translateX(3px);
            box-shadow: 0 5px 15px rgba(0, 212, 255, 0.4);
            color: #ffffff;
            text-decoration: none;
        }
        
        /* Empty State */
        .empty-state {
            text-align: center;
            padding: 100px 30px;
        }
        
        .empty-icon {
            font-size: 120px;
            color: rgba(255, 255, 255, 0.1);
            margin-bottom: 30px;
        }
        
        .empty-title {
            font-size: 36px;
            font-weight: 800;
            margin-bottom: 15px;
        }
        
        .empty-message {
            font-size: 18px;
            color: rgba(255, 255, 255, 0.6);
            margin-bottom: 40px;
            max-width: 500px;
            margin-left: auto;
            margin-right: auto;
        }
        
        .browse-btn {
            display: inline-flex;
            align-items: center;
            gap: 12px;
            background: linear-gradient(135deg, var(--accent-teal), var(--accent-gold));
            border: none;
            padding: 18px 40px;
            border-radius: 50px;
            color: #ffffff;
            font-weight: 700;
            font-size: 18px;
            text-decoration: none;
            transition: all 0.3s ease;
            box-shadow: 0 10px 30px rgba(29, 209, 161, 0.3);
        }
        
        .browse-btn:hover {
            transform: translateY(-3px);
            box-shadow: 0 15px 40px rgba(29, 209, 161, 0.5);
            color: #ffffff;
            text-decoration: none;
        }
        
        @media (max-width: 768px) {
            .page-title { font-size: 36px; }
            .wishlist-grid { grid-template-columns: 1fr; }
        }
    </style>
</head>

<body>
    <nav class="glass-nav">
        <div class="nav-container">
            <a href="/home" class="logo">
                <div class="logo-icon"><i class="fas fa-globe-americas"></i></div>
                <span>Travel World</span>
            </a>
            
            <div class="nav-links">
                <a href="/home" class="nav-link-item"><i class="fas fa-home"></i> Home</a>
                <a href="/wanttogo" class="nav-link-item"><i class="fas fa-heart"></i> My List</a>
                
                <div class="user-dropdown">
                    <div class="user-avatar" onclick="toggleDropdown()">
                        <%= username.substring(0, 2).toUpperCase() %>
                    </div>
                    <div class="dropdown-menu-custom" id="userDropdown">
                        <div style="padding: 10px 15px; border-bottom: 1px solid rgba(255,255,255,0.1); margin-bottom: 10px;">
                            <div style="font-size: 12px; color: rgba(255,255,255,0.5);">Signed in as</div>
                            <div style="font-weight: 700; color: var(--accent-blue);"><%= username %></div>
                        </div>
                        <a href="/wanttogo" class="dropdown-item-custom">
                            <i class="fas fa-heart"></i> My Wishlist
                        </a>
                        <form method="POST" action="/logout" style="margin: 0;">
                            <button type="submit" class="dropdown-item-custom" style="width: 100%; text-align: left; background: none; border: none; cursor: pointer; color: rgba(255,255,255,0.9);">
                                <i class="fas fa-sign-out-alt"></i> Logout
                            </button>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </nav>
    
    <div class="page-header">
        <h1 class="page-title">💖 Your Dream Travel Wishlist</h1>
        <p class="page-subtitle">Your personalized collection of dream destinations</p>
        <div class="wishlist-stats">
            <i class="fas fa-map-marked-alt" style="color: var(--accent-teal);"></i>
            <span style="font-weight: 700;"><%= destinations.length %> <%= destinations.length === 1 ? 'Destination' : 'Destinations' %></span>
        </div>
    </div>
    
    <div class="wishlist-container">
        <% if (destinations.length > 0) { %>
            <div class="wishlist-grid">
                <% destinations.forEach(destination => { %>
                    <div class="wishlist-card" onclick="location.href='/<%= destination.name.toLowerCase().replace(' trail', '').replace(' ', '') %>'">
                        <div class="card-image-wrapper">
                            <img src="<%= destination.image %>" class="card-image" alt="<%= destination.name %>">
                            <div class="card-category"><%= destination.category %></div>
                        </div>
                        <div class="card-body-wishlist">
                            <h3 class="wishlist-name"><%= destination.name %></h3>
                            <p class="wishlist-description"><%= destination.description.substring(0, 120) %>...</p>
                            <div class="card-actions">
                                <a href="/<%= destination.name.toLowerCase().replace(' trail', '').replace(' ', '') %>" class="view-detail-btn">
                                    View Details <i class="fas fa-arrow-right"></i>
                                </a>
                            </div>
                        </div>
                    </div>
                <% }) %>
            </div>
        <% } else { %>
            <div class="empty-state">
                <div class="empty-icon">
                    <i class="fas fa-heart-broken"></i>
                </div>
                <h2 class="empty-title">Your Wishlist is Empty</h2>
                <p class="empty-message">
                    Start exploring amazing destinations and add them to your wishlist to plan your next adventure!
                </p>
                <a href="/home" class="browse-btn">
                    <i class="fas fa-compass"></i>
                    Browse Destinations
                </a>
            </div>
        <% } %>
    </div>
    
    <script>
        function toggleDropdown() {
            document.getElementById('userDropdown').classList.toggle('show');
        }
        
        document.addEventListener('click', function(event) {
            const dropdown = document.getElementById('userDropdown');
            const avatar = document.querySelector('.user-avatar');
            if (avatar && dropdown && !avatar.contains(event.target) && !dropdown.contains(event.target)) {
                dropdown.classList.remove('show');
            }
        });
    </script>
</body>
</html>