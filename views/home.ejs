<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Travel World - Your Next Adventure Awaits</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css"
        integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root {
            --primary-dark: #0a0e27;
            --secondary-dark: #151a35;
            --accent-blue: #00d4ff;
            --accent-gold: #ffd700;
            --accent-teal: #1dd1a1;
            --card-overlay: rgba(0, 0, 0, 0.6);
        }
        
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            background: var(--primary-dark);
            background-image: 
                radial-gradient(circle at 20% 50%, rgba(0, 212, 255, 0.1) 0%, transparent 50%),
                radial-gradient(circle at 80% 80%, rgba(29, 209, 161, 0.1) 0%, transparent 50%),
                url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1440 320"><path fill="%23151a35" fill-opacity="0.3" d="M0,96L48,112C96,128,192,160,288,160C384,160,480,128,576,122.7C672,117,768,139,864,149.3C960,160,1056,160,1152,138.7C1248,117,1344,75,1392,53.3L1440,32L1440,320L1392,320C1344,320,1248,320,1152,320C1056,320,960,320,864,320C768,320,672,320,576,320C480,320,384,320,288,320C192,320,96,320,48,320L0,320Z"></path></svg>');
            background-attachment: fixed;
            background-size: cover;
            min-height: 100vh;
            color: #ffffff;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            overflow-x: hidden;
        }
        
        /* Glassmorphism Navigation */
        .glass-nav {
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            z-index: 1000;
            background: rgba(21, 26, 53, 0.7);
            backdrop-filter: blur(20px);
            -webkit-backdrop-filter: blur(20px);
            border-bottom: 1px solid rgba(255, 255, 255, 0.1);
            padding: 15px 0;
            transition: all 0.3s ease;
        }
        
        .glass-nav:hover {
            background: rgba(21, 26, 53, 0.85);
        }
        
        .nav-container {
            max-width: 1400px;
            margin: 0 auto;
            padding: 0 30px;
            display: flex;
            align-items: center;
            justify-content: space-between;
        }
        
        .logo {
            display: flex;
            align-items: center;
            gap: 12px;
            font-size: 26px;
            font-weight: 800;
            color: #ffffff;
            text-decoration: none;
            transition: transform 0.3s ease;
        }
        
        .logo:hover {
            transform: scale(1.05);
            text-decoration: none;
            color: var(--accent-blue);
        }
        
        .logo-icon {
            width: 40px;
            height: 40px;
            background: linear-gradient(135deg, var(--accent-blue), var(--accent-teal));
            border-radius: 10px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 20px;
        }
        
        .nav-links {
            display: flex;
            align-items: center;
            gap: 30px;
        }
        
        .nav-link-item {
            color: rgba(255, 255, 255, 0.8);
            text-decoration: none;
            font-weight: 600;
            transition: all 0.3s ease;
            position: relative;
            padding: 8px 0;
        }
        
        .nav-link-item:hover {
            color: var(--accent-blue);
            text-decoration: none;
        }
        
        .nav-link-item::after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 0;
            width: 0;
            height: 2px;
            background: var(--accent-blue);
            transition: width 0.3s ease;
        }
        
        .nav-link-item:hover::after {
            width: 100%;
        }
        
        .user-dropdown {
            position: relative;
        }
        
        .user-avatar {
            width: 42px;
            height: 42px;
            border-radius: 50%;
            background: linear-gradient(135deg, var(--accent-blue), var(--accent-teal));
            display: flex;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            font-weight: 700;
            font-size: 16px;
            transition: all 0.3s ease;
            border: 2px solid rgba(255, 255, 255, 0.2);
        }
        
        .user-avatar:hover {
            transform: scale(1.1);
            box-shadow: 0 0 20px rgba(0, 212, 255, 0.5);
        }
        
        .dropdown-menu-custom {
            position: absolute;
            top: 55px;
            right: 0;
            background: rgba(21, 26, 53, 0.95);
            backdrop-filter: blur(20px);
            border-radius: 15px;
            padding: 15px;
            min-width: 200px;
            box-shadow: 0 10px 40px rgba(0, 0, 0, 0.5);
            border: 1px solid rgba(255, 255, 255, 0.1);
            display: none;
            animation: slideDown 0.3s ease;
        }
        
        .dropdown-menu-custom.show {
            display: block;
        }
        
        .dropdown-item-custom {
            padding: 12px 15px;
            color: rgba(255, 255, 255, 0.9);
            text-decoration: none;
            display: block;
            border-radius: 8px;
            transition: all 0.3s ease;
            font-weight: 500;
        }
        
        .dropdown-item-custom:hover {
            background: rgba(0, 212, 255, 0.1);
            color: var(--accent-blue);
            text-decoration: none;
        }
        
        .search-icon {
            font-size: 20px;
            color: rgba(255, 255, 255, 0.8);
            cursor: pointer;
            transition: all 0.3s ease;
        }
        
        .search-icon:hover {
            color: var(--accent-blue);
            transform: scale(1.2);
        }
        
        /* Hero Section */
        .hero-section {
            margin-top: 100px;
            padding: 80px 30px 60px;
            text-align: center;
            position: relative;
        }
        
        .hero-title {
            font-size: 64px;
            font-weight: 900;
            background: linear-gradient(135deg, #ffffff, var(--accent-blue));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            margin-bottom: 20px;
            letter-spacing: -2px;
            animation: fadeInUp 0.8s ease;
        }
        
        .hero-subtitle {
            font-size: 24px;
            color: rgba(255, 255, 255, 0.7);
            margin-bottom: 50px;
            font-weight: 300;
            letter-spacing: 1px;
            animation: fadeInUp 0.8s ease 0.2s;
            animation-fill-mode: backwards;
        }
        
        /* Central Search Bar */
        .search-container {
            max-width: 700px;
            margin: 0 auto 80px;
            animation: fadeInUp 0.8s ease 0.4s;
            animation-fill-mode: backwards;
        }
        
        .search-wrapper {
            position: relative;
            background: rgba(255, 255, 255, 0.05);
            backdrop-filter: blur(10px);
            border-radius: 50px;
            padding: 8px;
            border: 2px solid rgba(255, 255, 255, 0.1);
            transition: all 0.3s ease;
        }
        
        .search-wrapper:focus-within {
            border-color: var(--accent-blue);
            box-shadow: 0 0 30px rgba(0, 212, 255, 0.3);
        }
        
        .search-input {
            width: 100%;
            padding: 18px 60px 18px 30px;
            background: transparent;
            border: none;
            color: #ffffff;
            font-size: 18px;
            outline: none;
        }
        
        .search-input::placeholder {
            color: rgba(255, 255, 255, 0.4);
        }
        
        .search-btn {
            position: absolute;
            right: 12px;
            top: 50%;
            transform: translateY(-50%);
            background: linear-gradient(135deg, var(--accent-blue), var(--accent-teal));
            border: none;
            padding: 14px 30px;
            border-radius: 40px;
            color: #ffffff;
            font-weight: 700;
            cursor: pointer;
            transition: all 0.3s ease;
        }
        
        .search-btn:hover {
            transform: translateY(-50%) scale(1.05);
            box-shadow: 0 5px 20px rgba(0, 212, 255, 0.4);
        }
        
        /* Destination Cards */
        .destinations-container {
            max-width: 1400px;
            margin: 0 auto;
            padding: 40px 30px;
        }
        
        .cards-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
            gap: 35px;
            margin-bottom: 60px;
        }
        
        .destination-card {
            position: relative;
            height: 450px;
            border-radius: 25px;
            overflow: hidden;
            cursor: pointer;
            transition: all 0.5s cubic-bezier(0.23, 1, 0.32, 1);
            box-shadow: 0 10px 40px rgba(0, 0, 0, 0.4);
        }
        
        .destination-card:hover {
            transform: translateY(-15px) scale(1.02);
            box-shadow: 0 20px 60px rgba(0, 212, 255, 0.3);
        }
        
        .card-bg {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-size: cover;
            background-position: center;
            transition: transform 0.5s ease;
        }
        
        .destination-card:hover .card-bg {
            transform: scale(1.1);
        }
        
        .card-overlay {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(to bottom, transparent 0%, var(--card-overlay) 70%);
            transition: all 0.5s ease;
        }
        
        .destination-card:hover .card-overlay {
            background: linear-gradient(to bottom, rgba(0, 0, 0, 0.3) 0%, rgba(0, 0, 0, 0.8) 70%);
        }
        
        .card-content {
            position: absolute;
            bottom: 0;
            left: 0;
            right: 0;
            padding: 35px;
            z-index: 10;
        }
        
        .card-icon {
            font-size: 40px;
            margin-bottom: 15px;
            display: inline-block;
            animation: float 3s ease-in-out infinite;
        }
        
        .card-title {
            font-size: 32px;
            font-weight: 800;
            margin-bottom: 12px;
            color: #ffffff;
        }
        
        .card-description {
            font-size: 16px;
            color: rgba(255, 255, 255, 0.8);
            margin-bottom: 20px;
            line-height: 1.6;
        }
        
        .card-btn {
            background: linear-gradient(135deg, var(--accent-blue), var(--accent-teal));
            border: none;
            padding: 14px 32px;
            border-radius: 25px;
            color: #ffffff;
            font-weight: 700;
            cursor: pointer;
            transition: all 0.3s ease;
            display: inline-block;
            text-decoration: none;
        }
        
        .card-btn:hover {
            transform: translateX(5px);
            box-shadow: 0 5px 20px rgba(0, 212, 255, 0.4);
            text-decoration: none;
            color: #ffffff;
        }
        
        /* Floating Wishlist Widget */
        .wishlist-widget {
            position: fixed;
            bottom: 30px;
            right: 30px;
            z-index: 999;
            animation: slideInRight 0.5s ease;
        }
        
        .wishlist-btn {
            background: linear-gradient(135deg, var(--accent-teal), var(--accent-gold));
            border: none;
            padding: 18px 35px;
            border-radius: 50px;
            color: #ffffff;
            font-weight: 700;
            font-size: 16px;
            cursor: pointer;
            box-shadow: 0 10px 30px rgba(29, 209, 161, 0.4);
            transition: all 0.3s ease;
            display: flex;
            align-items: center;
            gap: 12px;
            text-decoration: none;
        }
        
        .wishlist-btn:hover {
            transform: translateY(-5px);
            box-shadow: 0 15px 40px rgba(29, 209, 161, 0.6);
            text-decoration: none;
            color: #ffffff;
        }
        
        .wishlist-icon {
            font-size: 20px;
            animation: heartbeat 1.5s ease-in-out infinite;
        }
        
        /* Animations */
        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
        
        @keyframes slideDown {
            from {
                opacity: 0;
                transform: translateY(-10px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
        
        @keyframes slideInRight {
            from {
                opacity: 0;
                transform: translateX(100px);
            }
            to {
                opacity: 1;
                transform: translateX(0);
            }
        }
        
        @keyframes float {
            0%, 100% {
                transform: translateY(0);
            }
            50% {
                transform: translateY(-10px);
            }
        }
        
        @keyframes heartbeat {
            0%, 100% {
                transform: scale(1);
            }
            50% {
                transform: scale(1.2);
            }
        }
        
        /* Success Alert */
        .success-banner {
            position: fixed;
            top: 80px;
            left: 50%;
            transform: translateX(-50%);
            z-index: 1001;
            max-width: 600px;
            width: 90%;
            background: rgba(29, 209, 161, 0.95);
            backdrop-filter: blur(10px);
            padding: 20px 30px;
            border-radius: 15px;
            box-shadow: 0 10px 40px rgba(0, 0, 0, 0.3);
            animation: slideDown 0.5s ease;
        }
        
        /* Responsive */
        @media (max-width: 768px) {
            .hero-title {
                font-size: 42px;
            }
            
            .hero-subtitle {
                font-size: 18px;
            }
            
            .cards-grid {
                grid-template-columns: 1fr;
            }
            
            .nav-container {
                flex-wrap: wrap;
            }
            
            .nav-links {
                gap: 15px;
            }
            
            .wishlist-widget {
                bottom: 20px;
                right: 20px;
            }
            
            .wishlist-btn {
                padding: 15px 25px;
                font-size: 14px;
            }
        }
    </style>
</head>

<body>
    <!-- Glassmorphism Navigation -->
    <nav class="glass-nav">
        <div class="nav-container">
            <a href="/home" class="logo">
                <div class="logo-icon">
                    <i class="fas fa-globe-americas"></i>
                </div>
                <span>Travel World</span>
            </a>
            
            <div class="nav-links">
                <a href="/home" class="nav-link-item">
                    <i class="fas fa-home"></i> Home
                </a>
                <i class="fas fa-search search-icon" onclick="document.querySelector('.search-input').focus()"></i>
                
                <div class="user-dropdown">
                    <div class="user-avatar" onclick="toggleDropdown()">
                        <%= username.substring(0, 2).toUpperCase() %>
                    </div>
                    <div class="dropdown-menu-custom" id="userDropdown">
                        <div style="padding: 10px 15px; border-bottom: 1px solid rgba(255,255,255,0.1); margin-bottom: 10px;">
                            <div style="font-size: 12px; color: rgba(255,255,255,0.5);">Signed in as</div>
                            <div style="font-weight: 700; color: var(--accent-blue);"><%= username %></div>
                        </div>
                        <a href="/wanttogo" class="dropdown-item-custom">
                            <i class="fas fa-heart"></i> My Wishlist
                        </a>
                        <form method="POST" action="/logout" style="margin: 0;">
                            <button type="submit" class="dropdown-item-custom" style="width: 100%; text-align: left; background: none; border: none; cursor: pointer; color: rgba(255,255,255,0.9);">
                                <i class="fas fa-sign-out-alt"></i> Logout
                            </button>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </nav>
    
    <!-- Success Banner -->
    <% if (loginSuccess) { %>
        <div class="success-banner">
            <div style="display: flex; align-items: center; justify-content: space-between;">
                <div style="display: flex; align-items: center; gap: 15px;">
                    <i class="fas fa-check-circle" style="font-size: 24px;"></i>
                    <div>
                        <div style="font-weight: 700; font-size: 16px;">Login Successful!</div>
                        <div style="font-size: 14px; opacity: 0.9;">Welcome back, <%= username %>! Start exploring amazing destinations.</div>
                    </div>
                </div>
                <button onclick="this.parentElement.parentElement.style.display='none'" style="background: none; border: none; color: white; font-size: 24px; cursor: pointer; opacity: 0.8; transition: opacity 0.3s;" onmouseover="this.style.opacity='1'" onmouseout="this.style.opacity='0.8'">
                    <i class="fas fa-times"></i>
                </button>
            </div>
        </div>
    <% } %>

    <!-- Hero Section -->
    <div class="hero-section">
        <h1 class="hero-title">Your Next Adventure Awaits</h1>
        <p class="hero-subtitle">Curated Journeys to Inspire Your Wanderlust</p>
        
        <!-- Central Search Bar -->
        <div class="search-container">
            <form method="GET" action='/searchresults' class="search-wrapper">
                <input type="text" name="search" class="search-input" placeholder="Where would you like to go? Search destinations..." required>
                <button type="submit" class="search-btn">
                    <i class="fas fa-search"></i> Search
                </button>
            </form>
        </div>
    </div>
    
    <!-- Destination Cards -->
    <div class="destinations-container">
        <div class="cards-grid">
            <!-- Mountain Adventures Card -->
            <div class="destination-card" onclick="location.href='/hiking'">
                <div class="card-bg" style="background-image: url('https://images.unsplash.com/photo-1506905925346-21bda4d32df4?w=800&q=80');"></div>
                <div class="card-overlay"></div>
                <div class="card-content">
                    <div class="card-icon">⛰️</div>
                    <h3 class="card-title">Mountain Adventures</h3>
                    <p class="card-description">Conquer majestic peaks and explore breathtaking mountain trails across the world's most stunning ranges.</p>
                    <span class="card-btn">
                        Explore Mountains <i class="fas fa-arrow-right"></i>
                    </span>
                </div>
            </div>
            
            <!-- Urban Exploration Card -->
            <div class="destination-card" onclick="location.href='/cities'">
                <div class="card-bg" style="background-image: url('https://images.unsplash.com/photo-1480714378408-67cf0d13bc1b?w=800&q=80');"></div>
                <div class="card-overlay"></div>
                <div class="card-content">
                    <div class="card-icon">🏙️</div>
                    <h3 class="card-title">Urban Exploration</h3>
                    <p class="card-description">Experience vibrant city life, iconic landmarks, and rich cultural heritage in the world's greatest metropolises.</p>
                    <span class="card-btn">
                        Discover Cities <i class="fas fa-arrow-right"></i>
                    </span>
                </div>
            </div>
            
            <!-- Beach Paradise Card -->
            <div class="destination-card" onclick="location.href='/islands'">
                <div class="card-bg" style="background-image: url('https://images.unsplash.com/photo-1559827260-dc66d52bef19?w=800&q=80');"></div>
                <div class="card-overlay"></div>
                <div class="card-content">
                    <div class="card-icon">🏝️</div>
                    <h3 class="card-title">Beach Paradise</h3>
                    <p class="card-description">Unwind on pristine beaches with crystal-clear waters and golden sands in tropical island paradises.</p>
                    <span class="card-btn">
                        Find Islands <i class="fas fa-arrow-right"></i>
                    </span>
                </div>
            </div>
        </div>
    </div>
    
    <!-- Floating Wishlist Widget -->
    <div class="wishlist-widget">
        <a href="/wanttogo" class="wishlist-btn">
            <span class="wishlist-icon">❤️</span>
            <span>My Wishlist</span>
        </a>
    </div>
    
    <script src="https://code.jquery.com/jquery-3.3.1.slim.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.7/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js"></script>
    
    <script>
        // Toggle user dropdown
        function toggleDropdown() {
            const dropdown = document.getElementById('userDropdown');
            dropdown.classList.toggle('show');
        }
        
        // Close dropdown when clicking outside
        document.addEventListener('click', function(event) {
            const dropdown = document.getElementById('userDropdown');
            const avatar = document.querySelector('.user-avatar');
            
            if (!avatar.contains(event.target) && !dropdown.contains(event.target)) {
                dropdown.classList.remove('show');
            }
        });
        
        // Smooth scroll reveal for cards
        const observerOptions = {
            threshold: 0.1,
            rootMargin: '0px 0px -100px 0px'
        };
        
        const observer = new IntersectionObserver(function(entries) {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.style.opacity = '1';
                    entry.target.style.transform = 'translateY(0)';
                }
            });
        }, observerOptions);
        
        document.querySelectorAll('.destination-card').forEach(card => {
            card.style.opacity = '0';
            card.style.transform = 'translateY(50px)';
            card.style.transition = 'opacity 0.6s ease, transform 0.6s ease';
            observer.observe(card);
        });
        
        // Auto-hide success banner after 5 seconds
        setTimeout(function() {
            const banner = document.querySelector('.success-banner');
            if (banner) {
                banner.style.animation = 'fadeOut 0.5s ease';
                setTimeout(() => banner.style.display = 'none', 500);
            }
        }, 5000);
        
        // Add fadeOut animation
        const style = document.createElement('style');
        style.textContent = `
            @keyframes fadeOut {
                from { opacity: 1; transform: translateX(-50%) translateY(0); }
                to { opacity: 0; transform: translateX(-50%) translateY(-20px); }
            }
        `;
        document.head.appendChild(style);
        
        // Search input focus effect
        const searchInput = document.querySelector('.search-input');
        if (searchInput) {
            searchInput.addEventListener('focus', function() {
                this.parentElement.style.transform = 'scale(1.02)';
            });
            
            searchInput.addEventListener('blur', function() {
                this.parentElement.style.transform = 'scale(1)';
            });
        }
        
        // Parallax effect for background
        window.addEventListener('scroll', function() {
            const scrolled = window.pageYOffset;
            const hero = document.querySelector('.hero-section');
            if (hero) {
                hero.style.transform = `translateY(${scrolled * 0.5}px)`;
                hero.style.opacity = 1 - (scrolled / 800);
            }
        });
    </script>
</body>
</html>