<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title><%= destination.name %> - Travel World</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css"
        integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root {
            --primary-dark: #0a0e27;
            --secondary-dark: #151a35;
            --accent-blue: #00d4ff;
            --accent-teal: #1dd1a1;
            --accent-gold: #ffd700;
        }
        
        * { margin: 0; padding: 0; box-sizing: border-box; }
        
        body {
            background: var(--primary-dark);
            min-height: 100vh;
            color: #ffffff;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
        
        /* Glassmorphism Navigation */
        .glass-nav {
            position: fixed; top: 0; left: 0; right: 0; z-index: 1000;
            background: rgba(21, 26, 53, 0.9); backdrop-filter: blur(20px);
            border-bottom: 1px solid rgba(255, 255, 255, 0.1); padding: 15px 0;
        }
        
        .nav-container {
            max-width: 1400px; margin: 0 auto; padding: 0 30px;
            display: flex; align-items: center; justify-content: space-between;
        }
        
        .logo { display: flex; align-items: center; gap: 12px; font-size: 26px;
            font-weight: 800; color: #ffffff; text-decoration: none; }
        
        .logo-icon {
            width: 40px; height: 40px;
            background: linear-gradient(135deg, var(--accent-blue), var(--accent-teal));
            border-radius: 10px; display: flex; align-items: center; justify-content: center;
        }
        
        .nav-links { display: flex; align-items: center; gap: 30px; }
        
        .nav-link-item { color: rgba(255, 255, 255, 0.8); text-decoration: none;
            font-weight: 600; transition: all 0.3s ease; }
        
        .nav-link-item:hover { color: var(--accent-blue); text-decoration: none; }
        
        .user-avatar {
            width: 42px; height: 42px; border-radius: 50%;
            background: linear-gradient(135deg, var(--accent-blue), var(--accent-teal));
            display: flex; align-items: center; justify-content: center; cursor: pointer;
            font-weight: 700; border: 2px solid rgba(255, 255, 255, 0.2);
            transition: all 0.3s ease;
        }
        
        .user-avatar:hover { transform: scale(1.1); box-shadow: 0 0 20px rgba(0, 212, 255, 0.5); }
        
        .user-dropdown { position: relative; }
        
        .dropdown-menu-custom {
            position: absolute; top: 55px; right: 0; background: rgba(21, 26, 53, 0.95);
            backdrop-filter: blur(20px); border-radius: 15px; padding: 15px; min-width: 200px;
            box-shadow: 0 10px 40px rgba(0, 0, 0, 0.5); border: 1px solid rgba(255, 255, 255, 0.1);
            display: none;
        }
        
        .dropdown-menu-custom.show { display: block; }
        
        .dropdown-item-custom {
            padding: 12px 15px; color: rgba(255, 255, 255, 0.9); text-decoration: none;
            display: block; border-radius: 8px; transition: all 0.3s ease;
        }
        
        .dropdown-item-custom:hover {
            background: rgba(0, 212, 255, 0.1); color: var(--accent-blue); text-decoration: none;
        }
        
        /* Hero Image Section */
        .hero-image-container {
            margin-top: 70px;
            height: 60vh;
            min-height: 400px;
            position: relative;
            overflow: hidden;
        }
        
        .hero-image {
            width: 100%;
            height: 100%;
            object-fit: cover;
            filter: brightness(0.7);
        }
        
        .hero-overlay {
            position: absolute;
            bottom: 0;
            left: 0;
            right: 0;
            padding: 60px 30px 40px;
            background: linear-gradient(to top, rgba(10, 14, 39, 0.95), transparent);
        }
        
        .destination-title {
            font-size: 52px;
            font-weight: 900;
            color: #ffffff;
            margin-bottom: 15px;
            text-shadow: 0 4px 20px rgba(0, 0, 0, 0.5);
        }
        
        .breadcrumb-custom {
            display: flex;
            align-items: center;
            gap: 10px;
            color: rgba(255, 255, 255, 0.7);
            font-size: 14px;
        }
        
        .breadcrumb-custom a {
            color: var(--accent-blue);
            text-decoration: none;
            transition: color 0.3s ease;
        }
        
        .breadcrumb-custom a:hover {
            color: var(--accent-teal);
        }
        
        /* Content Section */
        .content-section {
            max-width: 1400px;
            margin: 0 auto;
            padding: 60px 30px;
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 60px;
        }
        
        /* Left Side - Description */
        .description-panel {
            background: var(--secondary-dark);
            border-radius: 25px;
            padding: 40px;
            border: 1px solid rgba(255, 255, 255, 0.1);
        }
        
        .section-label {
            color: var(--accent-blue);
            font-size: 14px;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 2px;
            margin-bottom: 20px;
            display: flex;
            align-items: center;
            gap: 10px;
        }
        
        .description-text {
            font-size: 18px;
            line-height: 1.8;
            color: rgba(255, 255, 255, 0.85);
            margin-bottom: 30px;
        }
        
        .key-facts {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 20px;
            margin-top: 30px;
        }
        
        .fact-item {
            background: rgba(0, 212, 255, 0.05);
            padding: 20px;
            border-radius: 15px;
            border: 1px solid rgba(0, 212, 255, 0.1);
        }
        
        .fact-label {
            color: rgba(255, 255, 255, 0.5);
            font-size: 12px;
            text-transform: uppercase;
            letter-spacing: 1px;
            margin-bottom: 8px;
        }
        
        .fact-value {
            color: var(--accent-blue);
            font-size: 18px;
            font-weight: 700;
        }
        
        /* Right Side - Media & Actions */
        .media-panel {
            display: flex;
            flex-direction: column;
            gap: 30px;
        }
        
        .video-container {
            background: var(--secondary-dark);
            border-radius: 25px;
            padding: 25px;
            border: 1px solid rgba(255, 255, 255, 0.1);
        }
        
        .video-wrapper {
            position: relative;
            padding-bottom: 56.25%;
            height: 0;
            overflow: hidden;
            border-radius: 15px;
        }
        
        .video-wrapper iframe {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            border: none;
        }
        
        .action-panel {
            background: linear-gradient(135deg, rgba(0, 212, 255, 0.1), rgba(29, 209, 161, 0.1));
            border-radius: 25px;
            padding: 40px;
            border: 1px solid rgba(0, 212, 255, 0.2);
            text-align: center;
        }
        
        .action-title {
            font-size: 24px;
            font-weight: 800;
            margin-bottom: 15px;
        }
        
        .action-subtitle {
            color: rgba(255, 255, 255, 0.7);
            margin-bottom: 30px;
            font-size: 15px;
        }
        
        .add-btn {
            width: 100%;
            background: linear-gradient(135deg, var(--accent-teal), var(--accent-gold));
            border: none;
            padding: 18px 40px;
            border-radius: 50px;
            color: #ffffff;
            font-weight: 700;
            font-size: 18px;
            cursor: pointer;
            transition: all 0.3s ease;
            box-shadow: 0 10px 30px rgba(29, 209, 161, 0.3);
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 12px;
        }
        
        .add-btn:hover {
            transform: translateY(-3px);
            box-shadow: 0 15px 40px rgba(29, 209, 161, 0.5);
        }
        
        .add-btn i {
            font-size: 22px;
        }
        
        /* Toast Notification */
        .toast-notification {
            position: fixed;
            bottom: 30px;
            right: 30px;
            z-index: 2000;
            background: rgba(21, 26, 53, 0.95);
            backdrop-filter: blur(20px);
            border-radius: 15px;
            padding: 20px 25px;
            box-shadow: 0 10px 40px rgba(0, 0, 0, 0.5);
            border-left: 4px solid;
            display: flex;
            align-items: center;
            gap: 15px;
            min-width: 350px;
            animation: slideInRight 0.5s ease;
        }
        
        .toast-notification.success {
            border-left-color: var(--accent-teal);
        }
        
        .toast-notification.error {
            border-left-color: #ff4757;
        }
        
        .toast-icon {
            width: 40px;
            height: 40px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 20px;
        }
        
        .toast-notification.success .toast-icon {
            background: rgba(29, 209, 161, 0.2);
            color: var(--accent-teal);
        }
        
        .toast-notification.error .toast-icon {
            background: rgba(255, 71, 87, 0.2);
            color: #ff4757;
        }
        
        .toast-content {
            flex: 1;
        }
        
        .toast-title {
            font-weight: 700;
            margin-bottom: 5px;
        }
        
        .toast-message {
            font-size: 14px;
            color: rgba(255, 255, 255, 0.7);
        }
        
        .toast-close {
            background: none;
            border: none;
            color: rgba(255, 255, 255, 0.5);
            font-size: 20px;
            cursor: pointer;
            transition: color 0.3s ease;
        }
        
        .toast-close:hover {
            color: #ffffff;
        }
        
        @keyframes slideInRight {
            from {
                opacity: 0;
                transform: translateX(100px);
            }
            to {
                opacity: 1;
                transform: translateX(0);
            }
        }
        
        @media (max-width: 968px) {
            .content-section {
                grid-template-columns: 1fr;
                gap: 40px;
            }
            
            .destination-title {
                font-size: 36px;
            }
            
            .key-facts {
                grid-template-columns: 1fr;
            }
            
            .toast-notification {
                min-width: auto;
                max-width: calc(100% - 40px);
            }
        }
    </style>
</head>

<body>
    <!-- Glassmorphism Navigation -->
    <nav class="glass-nav">
        <div class="nav-container">
            <a href="/home" class="logo">
                <div class="logo-icon"><i class="fas fa-globe-americas"></i></div>
                <span>Travel World</span>
            </a>
            
            <div class="nav-links">
                <a href="/home" class="nav-link-item"><i class="fas fa-home"></i> Home</a>
                <a href="/wanttogo" class="nav-link-item"><i class="fas fa-heart"></i> My List</a>
                
                <div class="user-dropdown">
                    <div class="user-avatar" onclick="toggleDropdown()">
                        <%= username.substring(0, 2).toUpperCase() %>
                    </div>
                    <div class="dropdown-menu-custom" id="userDropdown">
                        <div style="padding: 10px 15px; border-bottom: 1px solid rgba(255,255,255,0.1); margin-bottom: 10px;">
                            <div style="font-size: 12px; color: rgba(255,255,255,0.5);">Signed in as</div>
                            <div style="font-weight: 700; color: var(--accent-blue);"><%= username %></div>
                        </div>
                        <a href="/wanttogo" class="dropdown-item-custom">
                            <i class="fas fa-heart"></i> My Wishlist
                        </a>
                        <form method="POST" action="/logout" style="margin: 0;">
                            <button type="submit" class="dropdown-item-custom" style="width: 100%; text-align: left; background: none; border: none; cursor: pointer; color: rgba(255,255,255,0.9);">
                                <i class="fas fa-sign-out-alt"></i> Logout
                            </button>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </nav>
    
    <!-- Hero Image Section -->
    <div class="hero-image-container">
        <img src="<%= destination.image %>" class="hero-image" alt="<%= destination.name %>">
        <div class="hero-overlay">
            <div style="max-width: 1400px; margin: 0 auto; padding: 0 30px;">
                <div class="breadcrumb-custom">
                    <a href="/home"><i class="fas fa-home"></i> Home</a>
                    <i class="fas fa-chevron-right" style="font-size: 10px;"></i>
                    <a href="/<%= destination.category %>"><%= destination.category.charAt(0).toUpperCase() + destination.category.slice(1) %></a>
                    <i class="fas fa-chevron-right" style="font-size: 10px;"></i>
                    <span><%= destination.name %></span>
                </div>
                <h1 class="destination-title"><%= destination.name %></h1>
            </div>
        </div>
    </div>
    
    <!-- Content Section -->
    <div class="content-section">
        <!-- Left Side - Description -->
        <div class="description-panel">
            <div class="section-label">
                <i class="fas fa-info-circle"></i>
                About This Destination
            </div>
            <div class="description-text">
                <%= destination.description %>
            </div>
            
            <div class="key-facts">
                <div class="fact-item">
                    <div class="fact-label">Category</div>
                    <div class="fact-value"><%= destination.category.charAt(0).toUpperCase() + destination.category.slice(1) %></div>
                </div>
                <div class="fact-item">
                    <div class="fact-label">Best Time</div>
                    <div class="fact-value">Year Round</div>
                </div>
                <div class="fact-item">
                    <div class="fact-label">Rating</div>
                    <div class="fact-value">⭐ 4.8/5</div>
                </div>
                <div class="fact-item">
                    <div class="fact-label">Popularity</div>
                    <div class="fact-value">🔥 Trending</div>
                </div>
            </div>
        </div>
        
        <!-- Right Side - Media & Actions -->
        <div class="media-panel">
            <!-- Video Player -->
            <div class="video-container">
                <div class="section-label">
                    <i class="fas fa-play-circle"></i>
                    Watch Video Tour
                </div>
                <div class="video-wrapper">
                    <iframe src="https://www.youtube.com/embed/QPpHQwNspgw" 
                            allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" 
                            allowfullscreen></iframe>
                </div>
            </div>
            
            <!-- Add to Wishlist Panel -->
            <div class="action-panel">
                <h3 class="action-title">💖 Save to Your Wishlist</h3>
                <p class="action-subtitle">Add this destination to your travel bucket list</p>
                <form method="POST" action="/addToWantToGo">
                    <input type="hidden" name="destination" value="<%= destination.name %>">
                    <button type="submit" class="add-btn">
                        <i class="fas fa-heart"></i>
                        Add to My List
                    </button>
                </form>
            </div>
        </div>
    </div>
    
    <!-- Toast Notification -->
    <% if (message) { %>
        <div class="toast-notification <%= message.includes('added') || message.includes('Success') ? 'success' : 'error' %>" id="toast">
            <div class="toast-icon">
                <i class="fas <%= message.includes('added') || message.includes('Success') ? 'fa-check-circle' : 'fa-info-circle' %>"></i>
            </div>
            <div class="toast-content">
                <div class="toast-title"><%= message.includes('added') || message.includes('Success') ? 'Success!' : 'Already Added' %></div>
                <div class="toast-message"><%= message %></div>
            </div>
            <button class="toast-close" onclick="closeToast()">
                <i class="fas fa-times"></i>
            </button>
        </div>
    <% } %>
    
    <script>
        function toggleDropdown() {
            document.getElementById('userDropdown').classList.toggle('show');
        }
        
        document.addEventListener('click', function(event) {
            const dropdown = document.getElementById('userDropdown');
            const avatar = document.querySelector('.user-avatar');
            if (avatar && dropdown && !avatar.contains(event.target) && !dropdown.contains(event.target)) {
                dropdown.classList.remove('show');
            }
        });
        
        function closeToast() {
            const toast = document.getElementById('toast');
            if (toast) {
                toast.style.animation = 'slideInRight 0.5s ease reverse';
                setTimeout(() => toast.style.display = 'none', 500);
            }
        }
        
        // Auto-hide toast after 5 seconds
        setTimeout(function() {
            closeToast();
        }, 5000);
    </script>
</body>
</html>