<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Search Results - Travel World</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css"
        integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root {
            --primary-dark: #0a0e27;
            --secondary-dark: #151a35;
            --accent-blue: #00d4ff;
            --accent-teal: #1dd1a1;
            --accent-gold: #ffd700;
        }
        
        * { margin: 0; padding: 0; box-sizing: border-box; }
        
        body {
            background: var(--primary-dark);
            background-image: radial-gradient(circle at 20% 50%, rgba(0, 212, 255, 0.1) 0%, transparent 50%),
                radial-gradient(circle at 80% 80%, rgba(29, 209, 161, 0.1) 0%, transparent 50%);
            background-attachment: fixed;
            min-height: 100vh;
            color: #ffffff;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
        
        .glass-nav {
            position: fixed; top: 0; left: 0; right: 0; z-index: 1000;
            background: rgba(21, 26, 53, 0.7); backdrop-filter: blur(20px);
            border-bottom: 1px solid rgba(255, 255, 255, 0.1); padding: 15px 0;
        }
        
        .nav-container {
            max-width: 1400px; margin: 0 auto; padding: 0 30px;
            display: flex; align-items: center; justify-content: space-between;
        }
        
        .logo { display: flex; align-items: center; gap: 12px; font-size: 26px;
            font-weight: 800; color: #ffffff; text-decoration: none; }
        
        .logo-icon {
            width: 40px; height: 40px;
            background: linear-gradient(135deg, var(--accent-blue), var(--accent-teal));
            border-radius: 10px; display: flex; align-items: center; justify-content: center;
        }
        
        .nav-links { display: flex; align-items: center; gap: 30px; }
        
        .nav-link-item {
            color: rgba(255, 255, 255, 0.8); text-decoration: none; font-weight: 600;
            transition: all 0.3s ease;
        }
        
        .nav-link-item:hover { color: var(--accent-blue); text-decoration: none; }
        
        .user-avatar {
            width: 42px; height: 42px; border-radius: 50%;
            background: linear-gradient(135deg, var(--accent-blue), var(--accent-teal));
            display: flex; align-items: center; justify-content: center; cursor: pointer;
            font-weight: 700; border: 2px solid rgba(255, 255, 255, 0.2);
            transition: all 0.3s ease;
        }
        
        .user-avatar:hover { transform: scale(1.1); box-shadow: 0 0 20px rgba(0, 212, 255, 0.5); }
        
        .user-dropdown { position: relative; }
        
        .dropdown-menu-custom {
            position: absolute; top: 55px; right: 0; background: rgba(21, 26, 53, 0.95);
            backdrop-filter: blur(20px); border-radius: 15px; padding: 15px; min-width: 200px;
            box-shadow: 0 10px 40px rgba(0, 0, 0, 0.5); border: 1px solid rgba(255, 255, 255, 0.1);
            display: none;
        }
        
        .dropdown-menu-custom.show { display: block; }
        
        .dropdown-item-custom {
            padding: 12px 15px; color: rgba(255, 255, 255, 0.9); text-decoration: none;
            display: block; border-radius: 8px; transition: all 0.3s ease;
        }
        
        .dropdown-item-custom:hover {
            background: rgba(0, 212, 255, 0.1); color: var(--accent-blue); text-decoration: none;
        }
        
        .search-header {
            margin-top: 100px;
            padding: 60px 30px 40px;
            text-align: center;
        }
        
        .search-title {
            font-size: 42px;
            font-weight: 900;
            color: #ffffff;
            margin-bottom: 15px;
        }
        
        .search-query {
            color: var(--accent-blue);
        }
        
        .results-count {
            display: inline-flex;
            align-items: center;
            gap: 10px;
            background: rgba(0, 212, 255, 0.1);
            padding: 10px 20px;
            border-radius: 25px;
            border: 1px solid rgba(0, 212, 255, 0.2);
            font-size: 14px;
            margin-top: 20px;
        }
        
        .results-container {
            max-width: 1200px;
            margin: 40px auto;
            padding: 0 30px 80px;
        }
        
        .results-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(350px, 1fr));
            gap: 25px;
        }
        
        .result-card {
            background: var(--secondary-dark);
            border-radius: 20px;
            overflow: hidden;
            cursor: pointer;
            transition: all 0.4s ease;
            border: 1px solid rgba(255, 255, 255, 0.1);
        }
        
        .result-card:hover {
            transform: translateY(-8px);
            box-shadow: 0 15px 40px rgba(0, 212, 255, 0.3);
            border-color: var(--accent-blue);
        }
        
        .result-image-wrapper {
            height: 220px;
            overflow: hidden;
            position: relative;
        }
        
        .result-image {
            width: 100%;
            height: 100%;
            object-fit: cover;
            transition: transform 0.4s ease;
        }
        
        .result-card:hover .result-image {
            transform: scale(1.1);
        }
        
        .result-category {
            position: absolute;
            top: 15px;
            right: 15px;
            background: rgba(0, 212, 255, 0.9);
            backdrop-filter: blur(10px);
            padding: 6px 16px;
            border-radius: 20px;
            font-size: 12px;
            font-weight: 700;
            text-transform: uppercase;
        }
        
        .result-body {
            padding: 25px;
        }
        
        .result-name {
            font-size: 24px;
            font-weight: 800;
            color: #ffffff;
            margin-bottom: 12px;
        }
        
        .result-description {
            font-size: 14px;
            color: rgba(255, 255, 255, 0.6);
            line-height: 1.6;
            margin-bottom: 20px;
        }
        
        .view-btn {
            display: inline-flex;
            align-items: center;
            gap: 8px;
            background: linear-gradient(135deg, var(--accent-blue), var(--accent-teal));
            border: none;
            padding: 12px 28px;
            border-radius: 25px;
            color: #ffffff;
            font-weight: 700;
            text-decoration: none;
            transition: all 0.3s ease;
        }
        
        .view-btn:hover {
            transform: translateX(5px);
            box-shadow: 0 5px 20px rgba(0, 212, 255, 0.4);
            color: #ffffff;
            text-decoration: none;
        }
        
        /* Empty State */
        .empty-state {
            text-align: center;
            padding: 100px 30px;
        }
        
        .empty-icon {
            font-size: 120px;
            color: rgba(255, 255, 255, 0.1);
            margin-bottom: 30px;
        }
        
        .empty-title {
            font-size: 36px;
            font-weight: 800;
            margin-bottom: 15px;
        }
        
        .empty-message {
            font-size: 18px;
            color: rgba(255, 255, 255, 0.6);
            margin-bottom: 40px;
        }
        
        .back-btn {
            display: inline-flex;
            align-items: center;
            gap: 12px;
            background: linear-gradient(135deg, var(--accent-blue), var(--accent-teal));
            border: none;
            padding: 15px 35px;
            border-radius: 50px;
            color: #ffffff;
            font-weight: 700;
            text-decoration: none;
            transition: all 0.3s ease;
        }
        
        .back-btn:hover {
            transform: translateY(-3px);
            box-shadow: 0 10px 30px rgba(0, 212, 255, 0.4);
            color: #ffffff;
            text-decoration: none;
        }
        
        @media (max-width: 768px) {
            .search-title { font-size: 28px; }
            .results-grid { grid-template-columns: 1fr; }
        }
    </style>
</head>

<body>
    <nav class="glass-nav">
        <div class="nav-container">
            <a href="/home" class="logo">
                <div class="logo-icon"><i class="fas fa-globe-americas"></i></div>
                <span>Travel World</span>
            </a>
            
            <div class="nav-links">
                <a href="/home" class="nav-link-item"><i class="fas fa-home"></i> Home</a>
                <a href="/wanttogo" class="nav-link-item"><i class="fas fa-heart"></i> My List</a>
                
                <div class="user-dropdown">
                    <div class="user-avatar" onclick="toggleDropdown()">
                        <%= username.substring(0, 2).toUpperCase() %>
                    </div>
                    <div class="dropdown-menu-custom" id="userDropdown">
                        <div style="padding: 10px 15px; border-bottom: 1px solid rgba(255,255,255,0.1); margin-bottom: 10px;">
                            <div style="font-size: 12px; color: rgba(255,255,255,0.5);">Signed in as</div>
                            <div style="font-weight: 700; color: var(--accent-blue);"><%= username %></div>
                        </div>
                        <a href="/wanttogo" class="dropdown-item-custom">
                            <i class="fas fa-heart"></i> My Wishlist
                        </a>
                        <form method="POST" action="/logout" style="margin: 0;">
                            <button type="submit" class="dropdown-item-custom" style="width: 100%; text-align: left; background: none; border: none; cursor: pointer; color: rgba(255,255,255,0.9);">
                                <i class="fas fa-sign-out-alt"></i> Logout
                            </button>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </nav>
    
    <div class="search-header">
        <% if (searchQuery) { %>
            <h1 class="search-title">Search Results for <span class="search-query">"<%= searchQuery %>"</span></h1>
            <div class="results-count">
                <i class="fas fa-search"></i>
                <span><%= searchResults.length %> <%= searchResults.length === 1 ? 'result' : 'results' %> found</span>
            </div>
        <% } else { %>
            <h1 class="search-title">🔍 Search Destinations</h1>
            <p style="color: rgba(255,255,255,0.7); font-size: 18px; margin-top: 15px;">Enter a destination name to start searching</p>
        <% } %>
    </div>
    
    <div class="results-container">
        <% if (searchQuery && searchResults.length > 0) { %>
            <div class="results-grid">
                <% searchResults.forEach(destination => { %>
                    <div class="result-card" onclick="location.href='/<%= destination.name.toLowerCase().replace(' trail', '').replace(' ', '') %>'">
                        <div class="result-image-wrapper">
                            <img src="<%= destination.image %>" class="result-image" alt="<%= destination.name %>">
                            <div class="result-category"><%= destination.category %></div>
                        </div>
                        <div class="result-body">
                            <h3 class="result-name"><%= destination.name %></h3>
                            <p class="result-description"><%= destination.description.substring(0, 120) %>...</p>
                            <a href="/<%= destination.name.toLowerCase().replace(' trail', '').replace(' ', '') %>" class="view-btn">
                                View Details <i class="fas fa-arrow-right"></i>
                            </a>
                        </div>
                    </div>
                <% }) %>
            </div>
        <% } else if (searchQuery && searchResults.length === 0) { %>
            <div class="empty-state">
                <div class="empty-icon">
                    <i class="fas fa-search-minus"></i>
                </div>
                <h2 class="empty-title">No Destinations Found</h2>
                <p class="empty-message">
                    We couldn't find any destinations matching "<%= searchQuery %>". Try searching with different keywords.
                </p>
                <a href="/home" class="back-btn">
                    <i class="fas fa-home"></i>
                    Back to Home
                </a>
            </div>
        <% } %>
    </div>
    
    <script>
        function toggleDropdown() {
            document.getElementById('userDropdown').classList.toggle('show');
        }
        
        document.addEventListener('click', function(event) {
            const dropdown = document.getElementById('userDropdown');
            const avatar = document.querySelector('.user-avatar');
            if (avatar && dropdown && !avatar.contains(event.target) && !dropdown.contains(event.target)) {
                dropdown.classList.remove('show');
            }
        });
    </script>
</body>
</html>